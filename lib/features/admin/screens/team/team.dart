import 'package:flutter/material.dart';

class AdminGlamTeamView extends StatelessWidget {
  const AdminGlamTeamView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Glam Team'),
    );
  }
}